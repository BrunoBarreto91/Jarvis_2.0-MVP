🧠 BLUEPRINT: DINÂMICA DE IDENTIDADE N8N1. O Fluxo de SegurançaAtualmente, o n8n ignora quem envia o comando. O novo padrão exige:Header: Authorization: Bearer <JWT_TOKEN> em todas as chamadas.Validação: O n8n deve verificar se o token é válido junto ao emissor (Cognito).2. Extração de UIDO ID único do usuário (UID) deve ser extraído do claim sub do token decodificado.Variável Global de Execução: {{ $node["Auth_Validator"].json["uid"] }}3. Alterações no Banco de Dados (MySQL)Todas as operações de CRUD devem ser filtradas:GET /tasks: SELECT * FROM tasks WHERE userId = :uidPOST /tasks: INSERT INTO tasks (userId, title, ...) VALUES (:uid, :title, ...)PATCH/DELETE: UPDATE/DELETE ... WHERE id = :id AND userId = :uid (Garante que um usuário não delete a task de outro).